# Database Configuration
DATABASE_URL="mysql://user:pass@localhost:3306/ciphervault"

# Authentication (BetterAuth)
BETTER_AUTH_SECRET="your_to_generate_secret"
BETTER_AUTH_URL="http://localhost:3000"

# Extension Integration
# Add chrome-extension://<EXT_ID> if you need specific origin trust
BETTER_AUTH_TRUSTED_ORIGINS="http://localhost:3000,chrome-extension://*"

# Security Constants
NEXT_PUBLIC_PBKDF2_ITERATIONS="100000"
NEXT_PUBLIC_ARGON2_ITERATIONS="2"
NEXT_PUBLIC_ARGON2_MEMORY="19456"
NEXT_PUBLIC_ARGON2_PARALLELISM="1"
NEXT_PUBLIC_HIBP_API_URL="https://api.pwnedpasswords.com/range/"
